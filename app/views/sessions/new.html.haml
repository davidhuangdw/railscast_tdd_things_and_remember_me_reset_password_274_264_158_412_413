.page-header
  %h1 Log In


= form_tag sessions_path do
  = input_for :email
  = input_for :password
  = input_for :remember_me, :check_box_tag

  .form-group
    = submit_tag :submit, class:'btn btn-primary'
